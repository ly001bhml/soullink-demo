# 开发时 Vite 会把 /api 转到此处；但「图生3D」只有在本变量与设置页里填的 API 地址 **同源（协议+主机+端口）一致** 时才走代理。
# 若 .env 仍是旧 IP（如 192.168.1.8）而设置页已是别的地址，图生3D 会直连设置页，不再误用 .env。
# VITE_FAY_API_URL=http://211.87.224.136:19500

# 若仅需单独指定代理目标（会优先于 VITE_FAY_API_URL），可改用：
# VITE_DEV_BACKEND=http://127.0.0.1:5000
